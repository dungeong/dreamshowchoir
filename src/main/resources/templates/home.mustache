<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1"/>
    <title>{{pageTitle}}</title>
    <link rel="stylesheet" href="/styles.css"/>
</head>
<body>
{{>partials/header}}

<main>

    <!-- HERO SECTION -->
    <section class="hero">
        <h1>울산 유일의 20~40대 청년퍼포먼스합창단<br>사회적협동조합 드림쇼콰이어입니다.</h1>
        <p class="hero__contact">문의전화 : <strong>010-5592-0970</strong></p>
    </section>

    <!-- QUICK LINKS -->
    <section class="quick-menu">
        {{#quicklinks}}
            <a class="quick" href="{{href}}">
                <h3>{{title}}</h3>
                <p>{{desc}}</p>
                <span class="arrow">→</span>
            </a>
        {{/quicklinks}}
    </section>

    <!-- MIDDLE SECTION -->
    <section class="middle">
        <div class="video-placeholder">
            <span class="icon">🎥</span>
        </div>

        <!-- NEWS LIST -->
        <div class="news">
            <div class="news__header">
                <h2>드림쇼콰이어 소식</h2>
                <a href="/news" class="news__more">+</a>
            </div>
            <ul class="news__list">
                {{#news}}
                    <li>
                        <a href="{{href}}">
                            <span class="news__title">{{title}}</span>
                            <time>{{date}}</time>
                        </a>
                    </li>
                {{/news}}
            </ul>
        </div>
    </section>

    <!-- BOTTOM LINKS -->
    <section class="bottom-links">
        {{#bottomLinks}}
            <a class="bottom-link" href="{{href}}">{{label}}</a>
        {{/bottomLinks}}
    </section>

</main>

{{>partials/footer}}
</body>
</html>
